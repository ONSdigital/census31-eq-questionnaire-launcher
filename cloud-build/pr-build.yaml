steps:
  # Build Docker image (PR-only, no push)
  - name: gcr.io/cloud-builders/docker
    id: docker-build-eq-launcher
    args:
      - build
      - "-t"
      - "launcher-pr:$SHORT_SHA"
      - .

options:
  logging: CLOUD_LOGGING_ONLY
