name: PR

on:
  pull_request:
    branches:
      - main

permissions: {}

jobs:
  build:
    permissions:
      contents: read
    runs-on: ubuntu-22.04
    steps:
      - name: Install Go
        uses: actions/setup-go@f111f3307d8850f501ac008e886eec1fd1932a34 # v5.3.0
        with:
          go-version: "1.25.x"
          cache: false
      - name: Checkout code
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - name: Cache dependencies
        id: cache-dependencies
        uses: actions/cache@1bd1e32a3bdc45362d1e726936510720a7c30a57 # v4.2.0
        with:
          path: ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
      - name: Install dependencies
        if: steps.cache-dependencies.outputs.cache-hit != 'true'
        run: go get
      - name: Build
        run: CGO_ENABLED=0 GOOS=linux go build -a -installsuffix cgo
#  docker-push:
#    permissions:
#      contents: read
#      id-token: write
#    runs-on: ubuntu-22.04
#    steps:
#      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
#
#      - id: auth
#        name: Authenticate to Google Cloud
#        uses: google-github-actions/auth@71f986410dfbc7added4569d411d040a91dc6935 # v2.1.8
#        with:
#          token_format: 'access_token'
#          workload_identity_provider: ${{ secrets.WORKLOAD_IDENTITY_PROVIDER }}
#          service_account: ${{ secrets.SERVICE_ACCOUNT }}
#
#      - name: Tag
#        run: |
#          CLEAN_TAG=$(echo "${{ github.event.pull_request.head.ref  }}"  | tr / -)
#          echo "TAG=$CLEAN_TAG" >> $GITHUB_ENV
#      - name: Build
#        run: docker build -t ${{ secrets.GAR_LOCATION }}/${{ secrets.GAR_PROJECT_ID }}/docker-images/census31-eq-questionnaire-launcher:$TAG .
#
#      - name: Push to GAR
#        run: |
#          gcloud auth configure-docker ${{ secrets.GAR_LOCATION }}
#          echo "Pushing to GAR with tag $TAG"
#          docker push ${{ secrets.GAR_LOCATION }}/${{ secrets.GAR_PROJECT_ID }}/docker-images/census31-eq-questionnaire-launcher:$TAG
